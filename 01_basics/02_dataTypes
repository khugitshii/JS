"use strict"; //treats all the JS code as a newer version

// alert("3 + 3") // we are using nodejs and not a browser

console.log(3 
    + 3); // code readibility should be high
console.log("Khushi");

let name = "Khushi"
let age = 22
let isLoggedIn = false
let city = null //empty
let gender // undefined

// 8 datatypes: string, number, bigint, undefined, null, symbol, and object 
//number datatype range=> 2 power 53
// bigint (for i=big values)
// string => ""
// boolean => true/false 
// null => standalone value
// undefined => 
// symbol => unique

// object(not a primitive datatype)

console.log(typeof "Khushi");
console.log(typeof age);
console.log(typeof undefined);  // output is undefined
console.log(typeof null); //type is object